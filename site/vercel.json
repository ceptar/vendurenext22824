{
    "version": 2,
    "builds": [
      {
        "src": "site/next.config.js",
        "use": "@vercel/next",
        "config": {
          "distDir": "site/.next"
        }
      }
    ],
    "buildCommand": "pnpm install && cd site && pnpm run build",
    "outputDirectory": "site/.next"
  }